<div class="container-fluid">

  <div class="navbar-header">
  <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
   </button>
    <a class="navbar-brand" href="{{ path('blog_homepage') }}">BoardNPlan</a>
  </div>

<div id="navbar" class="collapse navbar-collapse">
  <ul class="nav navbar-nav">
    {% if is_granted('ROLE_SUPER_ADMIN') %}
      <li class="active"> <a href="{{ path('blog_homepage') }}"> {{'visitor.version'|trans }} </a> </li>
    {% endif %}
    <li> <a href="{{ path('blog_homepage') }}">{{'home'|trans }}</a> </li>
  </ul>
    <ul class="nav navbar-top-links navbar-right">
     <p class="navbar-text pull-right"><!-- /Barre de navigation DROITE -->
            {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
            Connected as  {{ app.user.username }} -
            <a href="{{ path('fos_user_security_logout') }}">{{'logout'|trans }}</a>
            {% else %}
            <a href="{{ path('fos_user_security_login') }}"> {{'login'|trans }} </a> 
            {% endif %}
     </p>
    </ul>
</div><!--/.nav-collapse -->

</div>